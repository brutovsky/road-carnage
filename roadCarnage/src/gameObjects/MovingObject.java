package gameObjects;

import org.newdawn.slick.Image;

public class MovingObject extends GameObject{
    public MovingObject(Image image, float scale, int x, int y) {
        super(image, scale, x, y);
    }
}
